# 工具收敛验收清单

- [x] 下载/clone 任务不再自动进入 create_plan（工具 schema 与提示词回归覆盖）。
- [x] 未完成 Todo 不再阻止 final。
- [x] 普通 shell 安装、测试、构建可运行（命令策略与工具契约回归覆盖）。
- [x] 默认 build schema 已缩小且不含 run_tests。
- [x] 旧 transcript 的 create_plan / run_tests Part 可回放（实现继续保留注册与历史投影路径）。
- [x] 后端测试和构建通过。
- [ ] 真实浏览器任务通过。
